[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mouse-hub"
version = "1.1.0"
description = "Motor local de controle e automação de entrada para Linux, com mouse, teclado e hardware baseado em capabilities."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["mouse", "keyboard", "automation", "input", "linux", "logitech", "g403"]
classifiers = [
    "Environment :: X11 Applications :: Qt",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: Utilities",
]
dependencies = [
    # Emissão nativa de input (XTest/XRecord) e leitura do título da
    # janela ativa (Xlib) — usada pelo engine de automação e pelas
    # consultas de foco. Dependência de produção, não de CI.
    "python-xlib>=0.17",
    "PyQt5>=5.15",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools.packages.find]
include = ["mouse_hub*", "app*"]
exclude = ["tests*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
